class: ReloadTest
table: reload_tests
fields:
  id: UuidValue, defaultModel=random
  reloadSessionId: UuidValue?
  reloadSession: ReloadSession?, relation(optional)
  firearmId: UuidValue?
  firearm: Firearm?, relation(optional)
  testDate: DateTime
  shotsFired: int
  highestVelocityFps: double
  lowestVelocityFps: double
  averageVelocityFps: double
  powerFactor: double
  averageEnergy: double
  groupingMeasurement: double?
  crackedCasings: int
indexes:
  reload_test_session_id_idx:
    fields: reloadSessionId
  reload_test_firearm_id_idx:
    fields: firearmId