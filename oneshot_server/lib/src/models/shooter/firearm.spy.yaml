class: Firearm
table: firearms
fields:
  id: UuidValue, defaultModel=random
  userInfo: module:auth:UserInfo?, relation(optional)
  purpose: String      # Defense, Sport, Collection, Hunting
  type: String         # Pistol, Revolver, Rifle
  action: String
  usageType: String    # Permitted, Restricted
  serialNumber: String
  manufactureCountry: String
  manufacturer: String
  model: String
  caliber: String
  magazineCapacity: int
  barrelLength: String
  weight: double
  acquisitionDate: DateTime?
  purchasePrice: double?
  saleDate: DateTime?
  salePrice: double?
  condition: String
indexes:
  firearm_user_id_idx:
    fields: userInfoId
  firearm_serial_number_idx:
    fields: serialNumber
