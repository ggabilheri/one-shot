class: ServiceOrderItem
table: service_order_items
fields:
  id: UuidValue?,  defaultPersist=random
  serviceOrderId: UuidValue?
  serviceOrder: ServiceOrder?, relation
  description: String
  isStockPart: bool
  supplyPartId: UuidValue?
  supplyPart: SupplyStock?, relation
  servicePrice: double
indexes:
  service_order_item_order_id_idx:
    fields: serviceOrderId